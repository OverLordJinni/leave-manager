{
  "name": "leave-manager",
  "version": "2.1.0",
  "private": true,
  "description": "Leave Manager — Vercel (serverless API + Vite PWA) on Neon Postgres",
  "engines": { "node": "22.x" },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@simplewebauthn/server": "^13.3.1",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0"
  }
}
