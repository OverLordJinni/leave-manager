# Backend API base URL.
# Leave EMPTY everywhere for the Vercel deployment — the API is served from the
# same domain (relative /api/* calls), so no cross-origin config is needed.
# Local dev: leave empty too (the Vite proxy forwards /api → localhost:3001,
# or use `vercel dev` for a single origin).
VITE_API_URL=
